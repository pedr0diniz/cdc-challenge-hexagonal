version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: cdc-revision
      POSTGRES_USER: zinid
      POSTGRES_PASSWORD: 12345678
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
